name: Build firmware

on:
  push:
    branches:
      - "**"
    tags:
      - "**"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: "${{ github.ref }}"
          fetch-depth: 0

      - name: Build firmware (ESP-IDF 5.5.3 / esp32c3)
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.5.3
          target: esp32c3

      - name: List build artifacts
        run: |
          echo "=== build dir top ==="
          ls -la build/
          echo "=== all files under build/ (top 100) ==="
          find build -type f | sort | head -100
          echo "=== firmware outputs ==="
          find build \( -name '*.bin' -o -name '*.elf' -o -name '*.map' \) -type f | sort | tee build/_firmware_filelist.txt
          shopt -s nullglob
          arr=(build/*.bin build/**/*.bin)
          echo "=== bin glob count: ${#arr[@]} ==="

      - name: Upload diagnostics (branch push)
        if: github.ref_type == 'branch'
        uses: actions/upload-artifact@v4
        with:
          name: diag-${{ github.ref_name }}
          path: build/_firmware_filelist.txt
          if-no-files-found: warn

      - name: Upload artifact (branch push)
        if: github.ref_type == 'branch'
        uses: actions/upload-artifact@v4
        with:
          name: passport-${{ github.ref_name }}
          path: |
            build/**/*.bin
            build/*.bin
            build/**/*.elf
            build/*.elf

      - name: Create release (tag push)
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v2
        with:
          name: "${{ github.ref_name }}"
          files: |
            build/**/*.bin
            build/*.bin
            build/**/*.elf
            build/*.elf
